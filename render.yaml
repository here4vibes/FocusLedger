services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && npm run db:generate && npm run migrate && npm test
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
