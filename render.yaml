services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && npm run db:generate && npm test && npm run migrate
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
