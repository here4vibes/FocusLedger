services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && npm run db:generate && npm test
    startCommand: npm run migrate && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
